{% extends 'base.html.twig' %}

{% block title %}Episode{% endblock %}

{% block body %}
    <h1>Episode {{ episode.number }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ episode.slug }}</td>
            </tr>
            <tr>
                <th>Season</th>
                <td>{{ episode.season.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ episode.title }}</td>
            </tr>
            <tr>
                <th>Numéro</th>
                <td>{{ episode.number }}</td>
            </tr>
            <tr>
                <th>Synopsis</th>
                <td>{{ episode.synopsis }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('program_index') }}">Retour</a>

    <div>
    <a class="btn" href="{{ path('episode_edit', {'slug': episode.slug}) }}">Modifier</a>

    {{ include('episode/_delete_form.html.twig') }}
    </div>

{% endblock %}
